# pager

Message delivery between coding-agent sessions — across projects, across tools.

Like a pager: a sender leaves a message, the recipient sees it the next time they
are active. No polling, no forced interruption.

Status: design. See `.agents/plans/`.
